# Dependencies (nặng nhất — hàng trăm MB)
node_modules/

# Secrets
.env

# Generated at runtime (không commit)
# Chú ý: phải có "/" đầu — "output/" trần sẽ match cả src/output/ (code)!
/output/
data/scraped/
data/images/
data/sent_deals.json
data/gemini-usage.json
# content-cache.json KHÔNG ignore (hướng A): commit về repo để local + CI
# dùng chung bài AI đã sinh → không tốn quota trùng lặp giữa các lần chạy.

# Misc
*.log
.DS_Store
scratchpad/
