# === Rakuten Hunter — secrets ===
# Phase 0-4 KHÔNG cần bất kỳ key nào (dùng fallback template + LocalSink).
# Chỉ điền khi tới phase tương ứng.

# Phase 3 (tùy chọn) — AI content. Bỏ trống = dùng fallback template.
GEMINI_API_KEY=

# Phase 5 — Telegram (HOÃN, chưa cần bây giờ)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHANNEL_ID=
